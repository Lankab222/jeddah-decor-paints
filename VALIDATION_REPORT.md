# تقرير التحقق من المشروع

## الفحوصات التي اجتازها المشروع

- التحقق من صحة ملفات JSON.
- التحقق من صحة ملف إعداد Decap CMS بصيغة YAML.
- التحقق من Frontmatter لجميع ملفات Markdown وعددها 23 ملفًا.
- التحقق من وجود جميع الصور المشار إليها داخل `/public/uploads/`.
- التحقق من عدم بقاء بيانات الموقع السابق أو رقمه أو روابطه.
- التحقق من عدم تضمين ملفات خطوط محلية داخل المشروع.
- التحقق من سلامة الصياغة النحوية لـ TypeScript في ملفات `.ts` وفي Frontmatter وسكربتات مكونات Astro.
- التحقق من إعدادات الهاتف وواتساب واسم الموقع والمدينة.
- التحقق من عدم تضمين `node_modules` أو `dist` أو `.astro` في نسخة التسليم.

## ملاحظة عن اختبار البناء

تعذر تنفيذ `npm install` ثم `npm run build` بالكامل داخل بيئة إعداد الملف لأن مرآة حزم npm الداخلية المتاحة في هذه البيئة لم تحتوِ على بعض الحزم التابعة، ومنها `zwitch` و`@astrojs/rss`. لم يظهر خطأ برمجي من ملفات المشروع أثناء الفحوصات الساكنة المذكورة أعلاه.

قبل النشر نفّذ في جهاز متصل بسجل npm العام:

```bash
npm install
npm run build
npm run preview
```

ثم اتبع `DEPLOY_CHECKLIST.md`.
